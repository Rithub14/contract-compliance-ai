OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# OCR engine: "auto" (default) uses Apple Vision on macOS, RapidOCR on Linux
# Set to "rapidocr" on macOS to test the Linux OCR path locally
OCR_ENGINE=auto
